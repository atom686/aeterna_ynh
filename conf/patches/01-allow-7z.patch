--- a/backend/internal/services/validation.go	2026-04-02 21:48:33.000000000 +0000
+++ b/backend/internal/services/validation.go	2026-04-29 07:16:06.960000211 +0000
@@ -32,6 +32,7 @@
 	".gif":  true,
 	".webp": true,
 	".zip":  true,
+	".7z":   true,
 }
 
 var AllowedMIMEPrefixes = []string{
@@ -44,6 +45,7 @@
 	"image/gif",
 	"image/webp",
 	"application/zip",
+	"application/x-7z-compressed",
 	"application/octet-stream", // fallback for unknown binary types like .docx
 }
 
@@ -175,7 +177,7 @@
 		return BadRequest("File must have an extension", nil)
 	}
 	if !AllowedExtensions[ext] {
-		return BadRequest("File type not allowed. Allowed: PDF, TXT, DOC, DOCX, JPG, PNG, GIF, WEBP, ZIP", nil)
+		return BadRequest("File type not allowed. Allowed: PDF, TXT, DOC, DOCX, JPG, PNG, GIF, WEBP, ZIP, 7Z", nil)
 	}
 
 	// Check MIME type via content sniffing (first 512 bytes)
--- a/frontend/src/components/CreateSwitch.jsx	2026-04-02 21:48:33.000000000 +0000
+++ b/frontend/src/components/CreateSwitch.jsx	2026-04-29 07:16:06.962000211 +0000
@@ -8,7 +8,7 @@
 import { Select } from "@/components/ui/select"
 import { apiRequest, uploadFile } from "@/lib/api"
 
-const ALLOWED_EXTENSIONS = ['.pdf', '.txt', '.doc', '.docx', '.jpg', '.jpeg', '.png', '.gif', '.webp', '.zip'];
+const ALLOWED_EXTENSIONS = ['.pdf', '.txt', '.doc', '.docx', '.jpg', '.jpeg', '.png', '.gif', '.webp', '.zip', '.7z'];
 const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10 MB
 const MAX_FILES = 5;
 const MAX_TOTAL_SIZE = 25 * 1024 * 1024; // 25 MB
@@ -122,7 +122,7 @@
     const validateFile = (file) => {
         const ext = '.' + file.name.split('.').pop().toLowerCase();
         if (!ALLOWED_EXTENSIONS.includes(ext)) {
-            return `"${file.name}" — type not allowed. Allowed: PDF, TXT, DOC, DOCX, JPG, PNG, GIF, WEBP, ZIP`;
+            return `"${file.name}" — type not allowed. Allowed: PDF, TXT, DOC, DOCX, JPG, PNG, GIF, WEBP, ZIP, 7Z`;
         }
         if (file.size > MAX_FILE_SIZE) {
             return `"${file.name}" exceeds 10 MB limit`;
@@ -424,7 +424,7 @@
                                     Drag & drop files or <span className="text-teal-400 underline">browse</span>
                                 </p>
                                 <p className="text-[10px] text-dark-600 mt-1">
-                                    PDF, TXT, DOC, images, ZIP • Max 10 MB each • {MAX_FILES} files max
+                                    PDF, TXT, DOC, images, ZIP, 7Z • Max 10 MB each • {MAX_FILES} files max
                                 </p>
                             </div>
 
--- a/frontend/src/components/Dashboard.jsx	2026-04-02 21:48:33.000000000 +0000
+++ b/frontend/src/components/Dashboard.jsx	2026-04-29 07:16:06.963000211 +0000
@@ -10,7 +10,7 @@
 import { Textarea } from "@/components/ui/textarea"
 import { Select } from "@/components/ui/select"
 
-const ALLOWED_EXTENSIONS = ['.pdf', '.txt', '.doc', '.docx', '.jpg', '.jpeg', '.png', '.gif', '.webp', '.zip'];
+const ALLOWED_EXTENSIONS = ['.pdf', '.txt', '.doc', '.docx', '.jpg', '.jpeg', '.png', '.gif', '.webp', '.zip', '.7z'];
 const MAX_FILE_SIZE = 10 * 1024 * 1024;
 const MAX_FILES = 5;
 const MAX_TOTAL_SIZE = 25 * 1024 * 1024;
